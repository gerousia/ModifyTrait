mlt_picture_decision = "gfx/interface/illustrations/decisions/decision_recruitment.dds"

mlt_blademaster_lifestyle_trait_gain_decision = {
	title = mlt_blademaster_lifestyle_trait_gain_decision
	picture = mlt_picture_decision
	major = yes
	ai_check_interval = 0
	cooldown = { days = 0 }

	is_shown = {
		has_ep2_dlc_trigger = yes
		# has_dlc_feature = tours_and_tournaments
		root = { NOT = { has_trait = lifestyle_blademaster } }
	} 

	is_valid_showing_failures_only = {
		root = { NOT = { has_trait = lifestyle_blademaster } } # Just in case it shows up
	}

	effect = {
		trigger_event = {
			id = mod_lifestyle_trait_track.1001
			days = 1
		}
		custom_tooltip = mlt_blademaster_lifestyle_trait_gain_decision_effect_tooltip
	}

	ai_potential = {
		is_ai = no
	}

	ai_will_do = {
		base = 0
	}
}

mlt_blademaster_lifestyle_trait_lose_decision = {
	title = mlt_blademaster_lifestyle_trait_lose_decision
	picture = mlt_picture_decision
	major = yes
	ai_check_interval = 0
	cooldown = { days = 0 }

	is_shown = {
		has_ep2_dlc_trigger = yes
		# has_dlc_feature = tours_and_tournaments
		root = { has_trait = lifestyle_blademaster }
	} 

	is_valid_showing_failures_only = {
		root = { has_trait = lifestyle_blademaster }# Just in case it shows up
	}

	effect = {
		trigger_event = {
			id = mod_lifestyle_trait_track.1001
			days = 1
		}
		custom_tooltip = mlt_blademaster_lifestyle_trait_gain_decision_effect_tooltip
	}

	ai_potential = {
		is_ai = no
	}

	ai_will_do = {
		base = 0
	}
}